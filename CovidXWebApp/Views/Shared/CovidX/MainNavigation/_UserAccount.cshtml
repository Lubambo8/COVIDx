@inject UserManager<EFCoreIdentityUser> _userManager;

@{
    var user = _userManager.GetUserAsync(User);
}

@if (user.Result is not null)
{
    <div class="user-panel mt-3 pb-3 mb-3 d-flex">
        <!--<div class="image">
            <img src="@user.Result.Avatar"
                 class="img-circle elevation-2"
                 alt="User Image">-->  <!--src="~/img/063-batman-img.svg"-->
        <!--</div>-->
        <div class="info">
            <a href="#"
               class="d-block">@user.Result.UserName</a>
        </div>
    </div>
}
else
{
    <div class="user-panel mt-3 pb-3 mb-3 d-flex">
        @*<div class="image">
            <img src="~/img/063-batman-img.svg"
                 class="img-circle elevation-2"
                 alt="User Image">
        </div>*@
        <div class="info">
            <a href="#"
               class="d-block">CO-X User</a>
        </div>
    </div>
}
