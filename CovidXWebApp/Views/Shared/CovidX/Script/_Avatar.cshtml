<script>

    // This is to prevent any jQuery code from running before the document is finished loading
    $(document).ready(function () {

        // find by id and add click event
        $('#onFemaleInidan').click(function () {
            // replace image tag
            $('#image-block').html('<img class="img-thumbnail elevation-2" src="img/avatar/female-indian.png" alt="User Avatar">');
        });

        // find by id and add click event
        $('#onFemaleNurse').click(function () {
            // replace image tag
            $('#image-block').html('<img class="img-thumbnail elevation-2" src="img/avatar/female-nurse.png" alt="User Avatar">');
        });

        // find by id and add click event
        $('#onMaleInidan').click(function () {
            // replace image tag
            $('#image-block').html('<img class="img-thumbnail elevation-2" src="img/avatar/male-indian.png" alt="User Avatar">');
        });

        // find by id and add click event
        $('#onMaleNurse').click(function () {
            // replace image tag
            $('#image-block').html('<img class="img-thumbnail elevation-2" src="img/avatar/male-nurse.png" alt="User Avatar">');
        });

        // find by id and add click event
        $('#onDevilMask').click(function () {
            // replace image tag
            $('#image-block').html('<img class="img-thumbnail elevation-2" src="img/avatar/trans-devil-mask.png" alt="User Avatar">');
        });

        // find by id and add click event
        $('#onReaper').click(function () {
            // replace image tag
            $('#image-block').html('<img class="img-thumbnail elevation-2" src="img/avatar/trans-reaper.png" alt="User Avatar">');
        });
    });

</script>
